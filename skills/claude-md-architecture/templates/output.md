# CLAUDE.md Architecture Report — <repo-name>

> Generated by claude-md-architecture. Evidence tags on every claim:
> `[DOC] [CONFIG] [CÓDIGO] [INFERENCIA] [SUPUESTO]`.

## 1. Summary
- Repo: `<path>`
- Before: root `CLAUDE.md` = `<N>` lines, loaded every turn. [CONFIG]
- After: root `<R>` lines (universals + imports) + `<M>` module files.
- Outcome: stable cacheable prefix + on-demand module rules.

## 2. Rule census and classification

| Rule (summary) | Current location | Target bucket | Target file |
|----------------|------------------|---------------|-------------|
| <rule> | root | universal | `CLAUDE.md` |
| <rule> | root | per-module | `<module>/CLAUDE.md` |
| <rule> | root | per-user | `~/.claude/CLAUDE.md` |

Duplicates found and deduped: `<list or none>`. [INFERENCIA]

## 3. Root team CLAUDE.md (stable prefix)

```markdown
# team CLAUDE.md (versioned, stable prefix)
@import ./CONVENTIONS.md
@import ~/.claude/CLAUDE.md

## Rules (universal)
- <universal rule>

## Path-scoped rules
- apply to: "<glob>/**"  ->  @import ./<module>/CLAUDE.md
```

Confirmed: no per-turn values, no subtree-only rules in the prefix. [INFERENCIA]

## 4. Module files

For each `<module>/CLAUDE.md`:
- Glob: `apply to: "<glob>/**"` (recursive). [DOC]
- Rules: `<list>`
- Created only because ≥1 own rule exists. [INFERENCIA]

## 5. Precedence table

| Glob (most → least specific) | Wins over | Tie-break |
|------------------------------|-----------|-----------|
| `src/api/**` | `src/**` | specificity |
| `<overlapping>` | `<other>` | `<explicit>` |

## 6. Import graph (DAG)

- Nodes: `<files>`
- Edges: `<@import relations>`
- Cycle check: PASS / FAIL. Broken-import check: PASS / FAIL (must fail loud). [DOC]

## 7. Upgrade safety
- Existing manual edits preserved: `<diff summary>`. [CÓDIGO]

## 8. Validation gate
- `bash skills/claude-md-architecture/scripts/check.sh` → `<functional output>`. [CONFIG]
- Acceptance checklist (SKILL.md): all boxes checked with evidence above.

## 9. Open items / assumptions
- `<assumption>` [SUPUESTO]
