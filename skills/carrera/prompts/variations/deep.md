# Prompt — deep variation (carrera, depth=deep)

Exhaustive path. Same single-playbook law, fuller treatment.

1. Resolve `topic`; ambiguous → one question, then stop.
2. Read `references/<topic>.md` only — still no sibling preload.
3. **Discover** every supplied evidence item; assign stable IDs; record source
   type; flag relative dates and missing fields as blockers, not defaults.
4. **Analyze** with all of the playbook's policy assets (e.g. acceptance filters
   + PIVOTE rubric + evidence + counteroffer policy for negotiation; status +
   evidence + date + risk policies for the selection board). Walk the relevant
   edge cases explicitly (floor-equals tie, exclusivity vs parallel stream,
   non-USD without conversion, duplicate `received_date`, weakest-dimension tie).
5. **Execute** the full contract: complete report, ranked/decision output,
   open questions, risk notes, and — where supported by supplied facts — a calm
   counterproposal or remediation step.
6. **Validate** through every guardian gate; run the validator and quote its
   exit code and output. Confirm byte-determinism reasoning (sort keys).
7. Document each non-obvious decision and trade-off with a single-family tag.
8. Stop and ask on any `{VACIO_CRITICO}`; never auto-fill to look complete.

Topic: {{topic}} · Evidence: {{evidence}}
