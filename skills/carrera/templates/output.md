# Carrera Deliverable — {{topic}}

**Routing trace:** topic=`{{topic}}` · depth=`{{quick|deep}}` · playbook=`references/{{topic}}.md`
**Tag family:** `{authoring [DOC][CONFIG][INFERENCIA][SUPUESTO][CÓDIGO] | provenance [EXPLICIT][INFERRED][OPEN]}`
**Status:** `{complete | partial | blocked}`

## 1. Request & scope
- Ask (one line):
- In scope for this topic / out of scope (named destination if redirected):

## 2. Evidence inventory
| ID | Source type | Received (ISO) | Short summary |
|----|-------------|----------------|---------------|
| E-001 | | YYYY-MM-DD | |

Relative dates preserved verbatim (blockers): …
Missing critical inputs (`{VACIO_CRITICO}`): …

## 3. Analysis (playbook-specific)
> Fill the block that matches the active playbook; delete the others.

- **Selection board:** stages (id, sequence, label, owner, status, status_reason,
  evidence_ref); contacts (name, role); exactly one next action → (stage + evidence).
- **Interview sim:** rubrics `substance` / `english` / `presence`, each with score
  [1–5] + verbatim snippet + rationale; weakest-dimension next step (no average).
- **Offer negotiation:** offer table with hard-filter pass/fail (floor, exclusivity,
  relocation, deal breakers) + PIVOTE [0–10 × 6]; ranking of passing offers only;
  open questions; optional counterproposal.
- **CV / onboarding / references / gratitude / closeout / acta / liquidación:**
  the playbook's contract fields and computed lines.

## 4. Open questions & risks
- Open question (no invented data):
- Risk → mitigation (evidence-backed only):

## 5. Recommendation / next action
- Single next action (resolves to a real evidence/stage/field):

## 6. Validation
- Validator run: `…` → exit `{0|n}`
- Determinism: sort keys honored (identical input ⇒ identical bytes): `{yes|no}`
- Guardian gates: single-playbook · contract-shape · tags-one-family ·
  no-invented-numbers · stop-on-empty · governance → `{pass|fail}`
