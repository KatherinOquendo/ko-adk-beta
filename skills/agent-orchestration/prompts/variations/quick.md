# Quick variation — agent-orchestration

`depth=quick`. Route fast, apply essentials only. [CONFIG]

1. Resolve `topic` to one of the 10 enum values (narrowest match). If two routes
   tie → ask one question; otherwise proceed. [CONFIG]
2. Load that one playbook. No cluster loading. [CONFIG]
3. Run the spine at essentials altitude: Discover the required inputs, Analyze
   with the playbook's core policy, Execute via `templates/output.md`, Validate
   with the Guardian checklist.
4. Gate: constitution v6.0.0 + evidence tags (single family) + script-first. [DOC]

Skip exhaustive enumeration of edge cases and alternative options — that is the
`deep` variation's job. Still never skip the Guardian gate or fabricate a "pass".
[DOC]

Example: "Which agent reviews this single Terraform diff?" → `triad-composition`,
quick: return Lead/Support/Guardian + execution mode (Triad) + G0–G3 gate names,
no committee analysis. [INFERENCE]
